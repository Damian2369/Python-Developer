# 1.Oblicz średnią arytmetyczną: przyjmij dwie liczby od użytkownika, następnie oblicz ich średnią arytmetyczną oraz wydrukuj ją na ekranie.

#Pobierz 2 liczby
liczba1 = float(input("Podaj pierwszą liczbę: "))
liczba2 = float(input("Podaj drugą liczbę: "))
#Oblicz średnią
srednia = (liczba1 + liczba2) / 2
#Wydrukuj wynik
print("Średnia arytmetyczna liczb {} i {} to: {}".format(liczba1, liczba2, srednia))

# 2.Podnieś do potęgi: przyjmij dwie liczby od użytkownika, podnieś pierwszą podaną liczbę do potęgi podanej jako druga liczba (przykładowo, jeśli
# podałem liczby 2 oraz 3, to wynik powinien wynosić 8. Przydatnym operatorem arytmetycznym będzie tu **).

#Pobierz 2 liczby
liczba1 = float(input("Podaj pierwszą liczbę: "))
liczba2 = float(input("Podaj drugą liczbę, do której chcesz podnieść pierwszą liczbę: "))
#Oblicz potęgę
wynik = liczba1 ** liczba2
#Wydrukuj wynik
print("{} podniesione do potęgi {} to: {}".format(liczba1, liczba2, wynik))

# 3.Porównaj liczby: przyjmij dwie liczby od użytkownika, a następnie wydrukuj na ekranie tę, która jest większa.
# Po zakończeniu którejkolwiek operacji, program powinien zacząć egzekucję od początku (przydatna będzie tutaj pętla while).

while True:
#Pobierz 2 liczby
liczba1 = float(input("Podaj pierwszą liczbę: "))
liczba2 = float(input("Podaj drugą liczbę: "))
#Porównaj 2 liczby i wydrukuj większą
if liczba1 > liczba2:
    print("Liczba {} jest większa od liczby {}".format(liczba1, liczba2))
elif liczba2 > liczba1:
    print("Liczba {} jest większa od liczby {}".format(liczba2, liczba1))
else:
    print("Liczby są równe.")
#Pytamy użytkownika czy chce kontynuować
kontynuacja = input("Czy chcesz poróœnać kolejne liczby? (Tak/Nie): ")
if kontynuacja.lower() != "tak":
    break

